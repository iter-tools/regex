/* @macrome
 * @generatedby /node_modules/@macrome/generator-typescript/lib/index.js
 * @generatedfrom ./index.ts#1644263074459
 * This file is autogenerated. Please do not edit it directly.
 * When editing run `npx macrome watch` then change the file this is generated from.
 */
import peekerate from 'iter-tools-es/methods/peekerate';
import { apiFactory } from './api-factory';
import { Engine } from './engine';
export { parse } from './regex';
export const {
  exec,
  test,
  execGlobal
} = apiFactory(function* generate(pattern, iterable) {
  const peekr = peekerate(iterable);
  const engine = new Engine(pattern);
  let value, done;

  try {
    ({
      value,
      done
    } = engine.step0(true, peekr.done, peekr.index, peekr.value));
    if (value !== null) yield* value;

    while (!done && !peekr.done) {
      engine.step1(peekr.value);
      peekr.advance();
      ({
        value,
        done
      } = engine.step0(false, peekr.done, peekr.index, peekr.value));
      if (value !== null) yield* value;
    }
  } finally {
    peekr.return();
  }
});